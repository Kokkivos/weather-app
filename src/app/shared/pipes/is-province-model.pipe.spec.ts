import { IsProvinceModelPipe } from './is-province-model.pipe';

describe('IsProvinceModelPipe', () => {
  it('create an instance', () => {
    const pipe = new IsProvinceModelPipe();
    expect(pipe).toBeTruthy();
  });
});
