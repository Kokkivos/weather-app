import { TransformModelToPipe } from './transform-model-to.pipe';

describe('TransformModelToPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformModelToPipe();
    expect(pipe).toBeTruthy();
  });
});
