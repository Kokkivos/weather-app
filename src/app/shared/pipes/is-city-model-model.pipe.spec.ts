import { IsCityModelModelPipe } from './is-city-model-model.pipe';

describe('IsCityModelModelPipe', () => {
  it('create an instance', () => {
    const pipe = new IsCityModelModelPipe();
    expect(pipe).toBeTruthy();
  });
});
